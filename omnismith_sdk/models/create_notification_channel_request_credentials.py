# coding: utf-8

"""
    Omnismith API

    API Documentation for Omnismith

    The version of the OpenAPI document: 1.0.11
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CreateNotificationChannelRequestCredentials(BaseModel):
    """
    Type-specific credentials payload (e.g. `bot_token` for Telegram; `url`, `auth_type`, `token`, `username`, `password`, `headers` for webhook)
    """ # noqa: E501
    bot_token: Optional[StrictStr] = Field(default=None, description="Telegram bot token from BotFather")
    url: Optional[StrictStr] = Field(default=None, description="Webhook destination URL")
    auth_type: Optional[StrictStr] = Field(default=None, description="Webhook HTTP authorization scheme")
    token: Optional[StrictStr] = Field(default=None, description="Bearer authentication token for webhook")
    username: Optional[StrictStr] = Field(default=None, description="Basic auth username for webhook")
    password: Optional[StrictStr] = Field(default=None, description="Basic auth password for webhook")
    headers: Optional[Dict[str, StrictStr]] = Field(default=None, description="Custom HTTP header key-value pairs to send with webhook POST requests")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["bot_token", "url", "auth_type", "token", "username", "password", "headers"]

    @field_validator('auth_type')
    def auth_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['none', 'bearer', 'basic']):
            raise ValueError("must be one of enum values ('none', 'bearer', 'basic')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateNotificationChannelRequestCredentials from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateNotificationChannelRequestCredentials from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "bot_token": obj.get("bot_token"),
            "url": obj.get("url"),
            "auth_type": obj.get("auth_type"),
            "token": obj.get("token"),
            "username": obj.get("username"),
            "password": obj.get("password"),
            "headers": obj.get("headers")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


